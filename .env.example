# =============================================================================
# YAYASAN MANAGEMENT SYSTEM — Environment Variables
# =============================================================================
# Copy file ini ke .env.local dan isi sesuai environment Anda
# JANGAN commit file .env.local ke git

# Database (PostgreSQL via Docker Compose)
DATABASE_URL="postgresql://yms_user:yms_password@localhost:5432/yms_db"

# Better Auth
BETTER_AUTH_URL="http://localhost:3000"
BETTER_AUTH_SECRET=""  # Generate: openssl rand -base64 32
